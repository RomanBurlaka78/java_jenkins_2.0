package roman.jenkins.model;

import org.openqa.selenium.WebDriver;
import roman.jenkins.model.base.BasePage;

public class CredentialProvidersPage extends BasePage<CredentialProvidersPage> {

    public CredentialProvidersPage(WebDriver driver) {
        super(driver);
    }
}
